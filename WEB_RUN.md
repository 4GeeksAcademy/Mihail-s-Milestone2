# Local Run Command

Use this command from the project root in Codespaces:

```bash
npx serve .
```

Then open the generated local URL and navigate to:

- `/index.html` for the landing page
- `/application.html` for the sign-up form
